apiVersion: v1
kind: Service
metadata:
  name: webapp-headless
  labels:
    app: webapp
spec:
  ports:
    - name: http
      port: 80
      protocol: TCP
  clusterIP: None
  selector:
    app: webapp